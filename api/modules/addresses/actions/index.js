/**
 * Point of contact for addresses actions.
 * @module modules/addresses/actions
 * @example import { lookup, search } from './actions';
 */

import lookup from './lookup';
import search from './search';

export {
  lookup,
  search,
};
