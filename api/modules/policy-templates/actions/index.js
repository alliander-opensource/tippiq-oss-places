/**
 * Point of contact for policy actions.
 * @module modules/policy-templates
 * @example import { getAllPolicyTemplates } from './actions';
 */

import getAllPolicyTemplates from './get-all-policy-templates';

export {
  getAllPolicyTemplates,
};
